@model List<Category>

@if(Model != null){
        <ul class="dropdown-menu">
            @foreach(var item in Model)
            {
                <li><a class="dropdown-item" href="/@item.Alias">@item.CastName</a></li>
            
            }


        </ul>
    
    
}